name: authentication_repository
description: Dart package which manages the authentication domain.
version: 1.0.0+3

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^1.0.2
  firebase_auth: ^1.0.1
  equatable: ^1.2.5
  meta: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.4